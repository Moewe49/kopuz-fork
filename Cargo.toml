[workspace]
members = [
   "crates/kopuz",
   "crates/components",
   "crates/hooks",
   "crates/pages",
   "crates/player",
   "crates/reader",
   "crates/server",
   "crates/utils",
   "crates/config",
   "crates/kopuz_route",
   "crates/discord-presence",
   "crates/scrobble",
   "crates/i18n",
   "crates/radio",
   "crates/embed",
   "crates/relay",
]
# The TLS-verifier shim is a `[patch]` crate, not a workspace member.
exclude = ["vendor/rustls-platform-verifier"]
resolver = "3"

# Replace rustls-platform-verifier with a WebPKI-roots shim so HTTPS works on
# Android (the real crate's JVM verifier panics unless JNI-initialized — which
# reqwest never does in our app). See vendor/rustls-platform-verifier/Cargo.toml.
[patch.crates-io]
rustls-platform-verifier = { path = "vendor/rustls-platform-verifier" }

[workspace.package]
version = "0.7.114"

[workspace.dependencies]
dioxus = { version = "0.7.9", features = [] }
getrandom = { version = "0.4", features = ["wasm_js"] }
web-sys = { version = "0.3", features = ["AudioContext", "AudioDestinationNode", "AudioNode", "AudioParam", "BiquadFilterNode", "BiquadFilterType", "ChannelMergerNode", "ChannelSplitterNode", "GainNode", "HtmlAudioElement", "HtmlMediaElement", "MediaElementAudioSourceNode", "MediaError", "Window", "Storage"] }
wasm-bindgen = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["rt", "time", "sync", "macros", "io-util"] }
async-recursion = "1.0"
discord-rich-presence = "1.1.0"
lofty = "0.24.0"
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "webp"] }
rfd = "0.17.2"
percent-encoding = "2.3.2"
dotenvy = "0.15"
http = "1.4.0"
uuid = { version = "1.20.0", features = ["v4", "fast-rng", "macro-diagnostics", "js"] }
cpal = "0.17"
symphonia = { version = "0.6", features = ["all"] }
rb = "0.4"
jellyfin-sdk-rust = "0.1.2"
reqwest = { version = "0.13", default-features = false, features = ["rustls", "http2", "charset", "query", "form"] }
rand = "0.10"
web-time = "1.1"
gloo-timers = { version = "0.4", features = ["futures"] }
directories = "6.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
tracing-appender = "0.2"
i18n = { path = "./crates/i18n" }
player = { path = "./crates/player" }
reader = { path = "./crates/reader" }
embed = { path = "./crates/embed" }
components = { path = "./crates/components" }
hooks = { path = "./crates/hooks" }
pages = { path = "./crates/pages" }
utils = { path = "./crates/utils" }
config = { path = "./crates/config" }
kopuz = { path = "./crates/kopuz" }
server = { path = "./crates/server" }
kopuz_route = { path = "./crates/kopuz_route" }
discord-presence = { path = "./crates/discord-presence" }
scrobble = { path = "./crates/scrobble" }
radio = { path = "./crates/radio" }
# Default features are off on purpose: neither reqwest nor axum should
# reach a device unless a crate has asked for that half by name.
relay = { path = "./crates/relay", default-features = false }

[profile.release]
strip = "symbols"
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"

[profile.dev.package."*"]
opt-level = 3

[profile.wasm-dev]
inherits = "dev"
opt-level = 1

[profile.server-dev]
inherits = "dev"

[profile.android-dev]
inherits = "dev"
opt-level = 1

# Release profile for shipping .aab / .apk. Thin LTO keeps link time sane on CI.
[profile.android-release]
inherits = "release"
strip = "symbols"
lto = "thin"
codegen-units = 1
panic = "abort"
